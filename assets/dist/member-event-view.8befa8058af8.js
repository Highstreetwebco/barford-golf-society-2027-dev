(()=>{"use strict";const t=window.BarfordMemberFlow;if(!t)return;const{esc:o}=t,c=(p,r,l=!1)=>`<button type="button" class="button ${l?"button-primary":"button-outline"}" data-member-action="${p}">${o(r)}</button>`;function B(p,r,{compact:l=!1}={}){const{event:e,rsvp:d,session:h,group:b,card:s}=r,u=t.nextAction(r),S=t.payment(e,d),k=["playing","reserve"].includes(d?.status),T=e.event_date>=t.today()&&e.status==="scheduled",$=l&&h&&e.event_date===t.today()&&e.status!=="cancelled"&&(s||d?.status==="playing"),Y=!($&&s&&(u.kind==="scorer"||u.href===t.scoreUrl(r)))&&(l||u.kind!=="link"||u.href!==t.eventUrl(e.id)),f=b.find(a=>a.is_you),v=f?.tee_time||b[0]?.tee_time,E=String(e.notes||"").split("[BARFORD_CANCEL_REASON]")[0].trim(),m=new Date(`${e.event_date}T12:00:00`),R=Number.isNaN(m.getTime())?"":`<div class="clubhouse-date-ticket" aria-hidden="true"><small>${m.toLocaleDateString("en-GB",{month:"short"})}</small><strong>${m.getDate()}</strong><small>${m.getFullYear()}</small></div>`,C=e.cancel_reason||String(e.notes||"").split("[BARFORD_CANCEL_REASON]")[1]?.trim();p.innerHTML=`${e.test_mode_active?'<p class="simple-notice"><strong>Practice event</strong> \u2014 this is a test round.</p>':""}
      ${$?`<section class="simple-card" id="dashboardScorecard" aria-labelledby="dashboardScorecardTitle">
        <p class="eyebrow">Playing today</p><h2 id="dashboardScorecardTitle">Today\u2019s scorecard</h2>
        <p><strong>${o(e.name)}</strong>${v?` \xB7 Tee off ${o(t.time(v))}`:""}</p>
        ${s?`<a class="button button-primary full-button" href="${t.scoreUrl(r)}">Open my group\u2019s scorecard</a>
        <p>${["submitted","locked"].includes(s.status)?"Your group\u2019s scores are available to view.":s.scorer_id===h.user.id?"You are keeping score. Open your card to start or continue your round.":s.scorer_id?"You can follow the scorecard here. Your chosen scorer enters the scores.":"Everyone in your group can open this card. Choose one person to enter the scores."}</p>
        ${s.status==="ready"&&!s.scorer_id?c("scorer","Choose our scorer"):""}`:`<p role="status">${r.cardError?"We couldn\u2019t load your scorecard. Try again to check it.":"Your scorecard will appear here when the committee has prepared your group."}</p>${c("refresh-card","Check for my scorecard")}`}
      </section>`:""}
      <article class="simple-card simple-next-event">
        <header class="clubhouse-event-heading"><div><p class="eyebrow">${$?"Today\u2019s event":l?"Your next event":"Your event"}</p>
        <h${l?"2":"1"}>${o(e.name)}</h${l?"2":"1"}>
        <p>${o(e.venue||"Venue to be confirmed")}</p></div>${R}</header>
        <div class="clubhouse-event-body"><p class="simple-event-date">${o(t.date(e.event_date))}</p>
        <div class="simple-next-action"><strong>${o(t.bookingLabel(r))}</strong><p>${o(u.message)}</p>${Y?c("primary",u.label,!0):""}</div>
        ${C&&e.status==="cancelled"?`<p>${o(C)}</p>`:""}
        <dl class="simple-facts" id="dashboardEventFacts"><div><dt>Your tee time</dt><dd>${v?o(t.time(v)):d?.status==="playing"?"Not announced yet":"Book to join a group"}</dd></div><div><dt>Event price</dt><dd>${o(t.money(e.price))}</dd></div>${k?`<div><dt>Payment</dt><dd>${o(S.label)}</dd></div><div><dt>Your request</dt><dd>${d.buggy_requested?"Buggy requested":"Walking"} \xB7 ${o(t.preference(d.preferred_tee_time))} tee time</dd></div>`:""}</dl>
        ${r.availability?`<p class="clubhouse-availability">${o(r.availability.playing_count||0)}${r.availability.capacity!=null?" of "+o(r.availability.capacity):""} places booked${r.availability.available!=null?" \xB7 "+o(r.availability.available)+" available":""}${r.availability.reserve_count?" \xB7 "+o(r.availability.reserve_count)+" on reserve":""}</p>`:""}
        ${l?`<a class="button button-outline full-button" href="${t.eventUrl(e.id)}">View all event details</a>`:""}
        </div>
      </article>
      ${l?"":`<section class="simple-card" id="my-group"><h2>My group</h2>${b.length?`<p><strong>Tee off at ${o(t.time(v))}${f?.tee_number?` \xB7 Tee ${o(f.tee_number)}`:""}</strong></p><ul class="simple-player-list">${b.map(a=>`<li>${a.photo_url?`<button class="group-photo" data-group-photo="${o(a.member_id)}" aria-label="View ${o(a.full_name||"member")} photo"><span aria-hidden="true">${o((a.full_name||"M").slice(0,1))}</span></button>`:""}${o(a.full_name||a.guest_name||"Guest")}${a.is_you?" (You)":""}${a.buggy_requested?" \xB7 Buggy requested":""}</li>`).join("")}</ul>`:`<p>${d?.status==="playing"?"Your group will appear here when the committee publishes the tee times.":d?.status==="reserve"?"Your group will be arranged if a place opens for you.":"Book a place to see your group here."}</p>`}${s?`<a class="button button-outline" href="${t.scoreUrl(r)}">${["submitted","locked"].includes(s.status)?"View group scores":"Open group scorecard"}</a>${s.status==="ready"?c("scorer","Choose or change our scorer"):""}`:r.cardError?"<p>Your scorecard could not be checked. Please refresh to try again.</p>":""}</section>`}
      <details class="simple-card simple-details"><summary>${l?"Booking and event options":"More event options"}</summary><div class="simple-detail-content">
        ${h&&T&&!r.bookingError?r.locked===!0?`<p>Bookings are closed. Ask the committee if you need to change your place or preferences.</p>${c("contact","Request a booking change")}`:r.locked===!1?`<div class="simple-actions">${c("book",k?"Change my booking":"Book my place")}${c("withdraw",d?.status==="reserve"?"Leave reserve list":"I can\u2019t play")}</div>`:"<p>Booking changes could not be checked. Please refresh.</p>":""}
        <div class="simple-actions">${h?c("roster","See who\u2019s playing"):""}${e.venue||e.address?c("directions","Get directions"):""}<a class="button button-outline" href="payments.html?event=${encodeURIComponent(e.id)}">Payment details</a><a class="button button-outline" href="scores.html?view=rounds&event=${encodeURIComponent(e.id)}">Round results</a><a class="button button-outline" href="gallery.html?event=${encodeURIComponent(e.id)}">Event photos</a>${e.course_video_url?c("video","Watch course video"):""}</div>
        <p>${e.first_tee_time?`First group tees off at ${o(t.time(e.first_tee_time))}. This may be different from your own tee time.`:""}</p>${e.address?`<p>${o(e.address)}</p>`:""}${E?`<p class="simple-notes">${o(E)}</p>`:""}
        ${h&&d?.status==="playing"&&e.event_date===t.today()?'<button id="dashboardEventCamera" class="button button-outline" type="button">Take event photo</button><input id="dashboardEventCameraInput" class="sr-only" type="file" accept="image/*" capture="environment"><p id="eventPhotoStatus" role="status"></p>':""}
      </div></details>`,p.querySelectorAll("[data-member-action]").forEach(a=>a.onclick=()=>{const n=a.dataset.memberAction;if(n==="primary")t.activate(u,r);else if(n==="book")t.openBooking(r);else if(n==="withdraw")t.withdraw(r);else if(n==="roster")t.showRoster(r);else if(n==="directions")t.directions(e);else if(n==="contact")t.contact(e);else if(n==="scorer")t.chooseScorer(r);else if(n==="refresh-card")window.dispatchEvent(new CustomEvent("barford-booking-changed",{detail:{eventId:e.id}}));else if(n==="video")try{const i=new URL(e.course_video_url);if(!["https:","http:"].includes(i.protocol))return;t.dialog("Course video",`<p>Open the course video for ${o(e.name)}.</p><a class="button button-primary" href="${o(i.href)}" target="_blank" rel="noopener">Watch video</a>`)}catch{}});const _=b.filter(a=>a.photo_url).map(a=>({...a,element:p.querySelector(`[data-group-photo="${a.member_id}"]`)})).filter(a=>a.element);_.length&&t.request(window.BarfordSupabase.storage.from("profile-images").createSignedUrls([...new Set(_.map(a=>a.photo_url))],3600)).then(a=>{const n=new Map((a||[]).filter(i=>!i.error&&i.signedUrl).map(i=>[i.path,i.signedUrl]));for(const i of _){const y=i.element,w=n.get(i.photo_url);if(!y||!w||!y.isConnected)continue;const g=document.createElement("img");g.src=w,g.alt="",g.loading="lazy",g.decoding="async",y.replaceChildren(g),y.dataset.profilePhoto=w}}).catch(()=>{}),window.BarfordDashboardModel=r,window.dispatchEvent(new CustomEvent("barford-dashboard-ready",{detail:r}))}window.BarfordEventView={render:B}})();
